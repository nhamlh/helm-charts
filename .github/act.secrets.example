# Copy this file to act.secrets and fill in real values.
# act.secrets is gitignored — never commit it.
#
# Get a token with repo scope from: https://github.com/settings/tokens
# Or use the gh CLI: gh auth token
GITHUB_TOKEN=your-github-personal-access-token
